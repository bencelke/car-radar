/** Canonical Firebase project ID for this repository. */
export const EXPECTED_FIREBASE_PROJECT_ID = "carradar-bd6fb";

/**
 * Next.js inlines `NEXT_PUBLIC_*` variables when the dev server starts.
 * After editing `.env.local`, stop the server, delete `.next`, and run `npm run dev` again.
 */
